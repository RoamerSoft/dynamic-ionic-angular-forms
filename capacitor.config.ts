import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Dynamic Ionic Angular Forms',
  webDir: 'www'
};

export default config;
